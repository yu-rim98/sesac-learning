# 깃 & 깃허브 특강

## 깃 명령어

### 브랜치 확인

`git branch`

### 브랜치 생성

`git branch <브랜치명>`

### 브랜치 전환

`git checkout <브랜치명>`  
`git switch <브랜치명>` - 가장 최근에 나옴

### 브랜치 생성 및 전환

`git checkout -b <브랜치명>`  
`git switch -c <브랜치명>`

### 브랜치 삭제

`git branch -d <브랜치명>`
